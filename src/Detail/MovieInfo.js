import styled from 'styled-components';

const MovieInfo = styled.div`
  display: flex;
  background: #ffffff;
  padding: 2rem 10%;
  > div {
    margin-left: 20px;
  }
  img {
    display: block;
    height: auto;
    position: relative;
    top: -5rem;
  }
`;

export default MovieInfo
